# malbolge-hello-hardmode

Minimal Malbolge verification repo.

`hello.mal` prints:

```text
Hello, World!
```

## Run

```sh
npm exec --yes --package malbolge-vm -- malbolge-interpreter hello.mal
```

## Language

The executable source is 100% Malbolge. Markdown is documentation only.
